apiVersion: v1
kind: Pod
metadata:
    name: "{{ include "knowhub.backend.name" . }}-test-connection"
    labels:
        {{- include "knowhub.backend.labels" . | nindent 8 }}
    annotations:
        "helm.sh/hook": test-success
spec:
    containers:
        - name: wget
          image: busybox
          command: ['wget']
          args:  ['{{ include "knowhub.backend.name" . }}:{{ .Values.service.port }}']
    restartPolicy: Never
