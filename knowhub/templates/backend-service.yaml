apiVersion: v1
kind: Service
metadata:
    name: {{ include "knowhub.backend.name" . }}
    labels:
        {{- include "knowhub.backend.labels" . | nindent 8 }}
spec:
    type: {{ .Values.service.type }}
    ports:
        - port: {{ .Values.service.sslPort }}
          protocol: TCP
          name: https
    selector:
        {{- include "knowhub.backend.matchLabels" . | nindent 8 }}
